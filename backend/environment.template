NODE_ENV=development
PORT=5000
APP_URL=http://localhost:5173
DATABASE_URL=
QUEUE_DATABASE_URL=
DATABASE_EXPECTED_ROLE=
DATABASE_CONNECT_TIMEOUT_SECONDS=5
DATABASE_STATEMENT_TIMEOUT_MS=30000
DATABASE_LOCK_TIMEOUT_MS=10000
DATABASE_IDLE_IN_TRANSACTION_TIMEOUT_MS=60000
DATABASE_SSLMODE=disable
DATABASE_SSL_REJECT_UNAUTHORIZED=true
MIGRATION_EXPECTED_ROLE=wpa_migrator
MIGRATION_REQUIRE_TLS=false
MIGRATION_DATABASE_SSLMODE=disable
MIGRATION_STATEMENT_TIMEOUT_MS=30000
MIGRATION_LOCK_TIMEOUT_MS=10000
MIGRATION_IDLE_IN_TRANSACTION_TIMEOUT_MS=60000
BETTER_AUTH_URL=http://localhost:5000
BETTER_AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Comma-separated browser origins allowed to call the API.
CORS_ORIGINS=http://localhost:5173
TRUST_PROXY=false

# Optional. When non-empty, analyze and AI routes require X-API-Key or Bearer auth.
API_KEYS=
# Required to enable DELETE /api/logs. Keep this separate from normal API keys.
ADMIN_API_KEYS=
# Only these existing Better Auth users may bootstrap the first super-admin role.
BOOTSTRAP_ADMIN_EMAILS=
# Local-only convenience. Production startup rejects true.
ADMIN_REAUTH_SESSION_LIFETIME=false
ADMIN_WEBAUTHN_REQUIRED=false
ADMIN_PRIVILEGED_ROLES=super_admin,admin
ADMIN_STEP_UP_MAX_AGE_SECONDS=600
ADMIN_WEBAUTHN_MIN_CREDENTIALS=1
ADMIN_WEBAUTHN_RECOMMENDED_CREDENTIALS=2
ADMIN_RECOVERY_CODE_COUNT=10
ADMIN_RECOVERY_SESSION_TTL_SECONDS=900
ADMIN_WEBAUTHN_ASSERTION_TTL_SECONDS=120

AI_PROVIDER=openrouter
AI_SERVICE_URL=http://127.0.0.1:5051
AI_SERVICE_TOKEN=
OPENROUTER_API_KEY=
OPENROUTER_MODEL_PRIMARY=
OPENROUTER_MODEL_FALLBACKS=
OPENROUTER_SITE_URL=http://localhost:5173
OPENROUTER_APP_NAME=WebPageAnalyzer
OPENROUTER_DATA_COLLECTION=deny
OPENROUTER_ZDR=false
OPENROUTER_METADATA_ENABLED=true
OPENROUTER_STRUCTURED_OUTPUT_MODE=native
OPENROUTER_REASONING_EFFORT=
AI_MAX_REQUESTS_PER_MINUTE=30
AI_MAX_CONCURRENT_REQUESTS=2
AI_MAX_INPUT_TOKENS=4096
AI_MAX_OUTPUT_TOKENS=1200
AI_DAILY_COST_SOFT_LIMIT_USD=5
AI_DAILY_COST_HARD_LIMIT_USD=10

# Production API -> isolated analysis-worker Engine Lab control channel.
ENGINE_LAB_SERVICE_URL=http://127.0.0.1:5030
ENGINE_LAB_SERVICE_TOKEN=
ENGINE_LAB_SERVICE_ALLOWED_HOSTS=127.0.0.1
ENGINE_LAB_SERVICE_ENABLED=false
ENGINE_LAB_SERVICE_HOST=127.0.0.1
ENGINE_LAB_SERVICE_PORT=5030
ENGINE_LAB_CONCURRENCY=3
ENGINE_LAB_MAX_CONCURRENT_RUNS=1
ENGINE_LAB_MAX_QUEUED_RUNS=8
ENGINE_LAB_HISTORY_LIMIT=50
ENGINE_LAB_ARTIFACT_TTL_MS=86400000
ENGINE_LAB_ARTIFACT_JANITOR_MS=900000
ENGINE_LAB_SHUTDOWN_DRAIN_MS=15000

REQUEST_BODY_LIMIT=32kb
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=120
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=60
LOGIN_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX=10
PASSWORD_RESET_RATE_LIMIT_WINDOW_MS=3600000
PASSWORD_RESET_RATE_LIMIT_MAX=5
ANALYZE_RATE_LIMIT_WINDOW_MS=3600000
ANALYZE_RATE_LIMIT_MAX=10
AI_RATE_LIMIT_WINDOW_MS=3600000
AI_RATE_LIMIT_MAX=20
REDEEM_RATE_LIMIT_WINDOW_MS=900000
REDEEM_RATE_LIMIT_MAX=10
ADMIN_MUTATION_RATE_LIMIT_WINDOW_MS=900000
ADMIN_MUTATION_RATE_LIMIT_MAX=60
ADMIN_PASSKEY_RATE_LIMIT_WINDOW_MS=900000
ADMIN_PASSKEY_RATE_LIMIT_MAX=20
ADMIN_RECOVERY_RATE_LIMIT_WINDOW_MS=900000
ADMIN_RECOVERY_RATE_LIMIT_MAX=5

MAX_CONCURRENT_ANALYSES=1
MAX_QUEUED_ANALYSES=8
SCAN_PROGRESS_RATE_LIMIT_WINDOW_MS=900000
SCAN_PROGRESS_RATE_LIMIT_MAX=600
ANALYSIS_TIMEOUT_MS=240000
LIGHTHOUSE_TIMEOUT_MS=180000
AXE_TIMEOUT_MS=90000
YELLOWLAB_TIMEOUT_MS=150000
AI_TIMEOUT_MS=45000
YELLOWLAB_MAX_POLL_ATTEMPTS=24
# Keep true in production: YellowLab receives a public target URL only after
# the user confirms the per-scan external-provider disclosure.
REQUIRE_EXTERNAL_PROVIDER_CONSENT=true

# Only 80 and 443 are recommended for a public deployment.
ALLOWED_TARGET_PORTS=80,443
PROXY_CONNECT_TIMEOUT_MS=10000
PROXY_MAX_CONNECTIONS=100
PROXY_MAX_RESPONSE_BYTES=26214400
PROXY_MAX_TOTAL_BYTES=262144000

ARTIFACT_DIR=logs
SOURCE_ARTIFACT_DIR=logs/source-inputs
WORKER_RESULT_DIR=logs/worker-results
KEEP_ANALYZER_ARTIFACTS=false
CHROME_PATH=
# Keep false: the isolated worker rejects this escape and probes the real sandbox.
CHROME_NO_SANDBOX=false

# Base64-encoded 32-byte key. Example generation: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SOURCE_ENCRYPTION_KEY=
SOURCE_UPLOAD_MAX_BYTES=52428800
WORKER_ENABLED=true
EXECUTION_ROLE=api
BROWSER_EXECUTION_DISABLED=false
PDF_EXECUTION_DISABLED=false
SOURCE_EXECUTION_DISABLED=false
OSV_EXECUTION_DISABLED=false
WORKER_SANDBOX_REQUIRED=true
WORKER_HANDLER_MODULE=platform/worker-handler.js

# Development defaults to paid-provider mode for focused adapter tests. Set
# false for Free + redeem-only operation; Paddle values may then remain empty.
PAYMENTS_ENABLED=true
BILLING_PROVIDER=paddle
ENTERPRISE_SALES_MODE=contact
PADDLE_ENVIRONMENT=sandbox
PADDLE_API_KEY=
PADDLE_WEBHOOK_SECRET=
PADDLE_PRICE_SIGNAL=
PADDLE_PRICE_STUDIO=
PADDLE_PRICE_ENTERPRISE=

# Production uses the isolated Resend email service. Local tests inject a mock.
EMAIL_PROVIDER=resend
EMAIL_SERVICE_URL=http://127.0.0.1:5052
EMAIL_SERVICE_TOKEN=
RESEND_API_KEY=
EMAIL_FROM=
SUPPORT_EMAIL=support@usewpa.tech
EMAIL_DELIVERY_ENABLED=true
EMAIL_PROVIDER_URL=https://api.resend.com/emails
EMAIL_PROVIDER_HOST_ALLOWLIST=api.resend.com

LEGAL_OPERATOR_NAME=
LEGAL_OPERATOR_TYPE=
LEGAL_COUNTRY=TR
LEGAL_BUSINESS_ADDRESS=
LEGAL_SUPPORT_EMAIL=support@usewpa.tech
LEGAL_SUPPORT_PHONE=
LEGAL_EFFECTIVE_DATE=
LEGAL_HOSTING_PROVIDER_NAME=
LEGAL_HOSTING_PROVIDER_REGION=
LEGAL_HOSTING_PROVIDER_PRIVACY_URL=
LEGAL_EDGE_PROVIDER_NAME=
LEGAL_EDGE_PROVIDER_REGION=
LEGAL_EDGE_PROVIDER_PRIVACY_URL=

CRAWLER_MAX_SITEMAPS=20
CRAWLER_MAX_SITEMAP_DEPTH=3
CRAWLER_MAX_SITEMAP_URLS=5000
CRAWLER_MAX_SITEMAP_BYTES=2097152
CRAWLER_MAX_SITEMAP_DECOMPRESSED_BYTES=8388608
CRAWLER_MAX_PATH_DEPTH=12
CRAWLER_MAX_QUERY_VARIANTS_PER_PATH=5

# Optional legacy development adapters only.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_SIGNAL=
STRIPE_PRICE_STUDIO=
STRIPE_PRICE_ENTERPRISE=
GEMINI_API_KEY=
GEMINI_MODEL=
